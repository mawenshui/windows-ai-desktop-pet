# 已有功能优化追踪

基线：0.14.0，2026-09-08；原问题按当前代码复核，编号保留。完整实施顺序见[扩展计划](后续功能扩展计划.md)。

| 编号 | 本次结果 | 剩余条件 |
| :--- | :--- | :--- |
| OPT-01 | 八组真实输入 runner 增补，不能前台激活时禁止输入/无关屏幕截图 | 两种执行会话仍 FAIL，解锁同桌面复测 |
| OPT-02 | 统一证据、过期/失败/缺失拒绝、受保护 Release、远端哈希复核 | 最终提交/硬件/签名/资产门禁全部 PASS 才发布 |
| OPT-03 | models 内容及模型验证、主动生成/取消、错误分类和响应预算 | 真实供应商按账户主动验证 |
| OPT-04 | 重启监听、子树、溢出串行、撤销任务和分页 Revision | 实际大磁盘/网络盘压力 |
| OPT-05 | 不复制活跃 SQLite，重启后重建，孤立图标清理保护引用 | 交互与长期运行复验 |
| OPT-06 | 维护接入实际 LocalAppData 日志根 | 双账户/重定向目录实测 |
| OPT-07 | 整包先校验、before/journal、失败与中断回滚、图标重定位 | 真升级、用户恢复和卸载两分支实测 |
| OPT-08 | 保存规则时区、错过合并、通知持久记录和提交/已读区分 | 睡眠/时钟/专注助手实测 |
| OPT-09 | 系统 runner 和物理 gate 分离 | 多屏、缩放、热插拔、Explorer、升级未齐 |
| OPT-10 | 损坏原文件保留、写覆盖拒绝、.previous 与迁移备份 | 手工损坏文件的用户恢复路径复验 |
| OPT-11 | 增补业务/SQLite/WPF/原型用例和七类证据收集 | 尚无独立覆盖率、格式、依赖漏洞阈值，不标通过 |
| OPT-12 | Directory.Build.props 统一版本，全量待处理计数用于诊断；本次 Release 全构建已核验 0.14.0.0 | 正式发布仍需其余门禁全部通过 |
| OPT-13 | 鼠标/Ctrl+Tab/提醒入口共用未保存导航保护，取消保留编辑 | 独立真实键盘/鼠标待验 |
| OPT-14 | 全局快捷键格式/重复校验与冲突整组回滚；托盘保留入口 | 解锁同桌面会话验证真实按键、占用及退出释放 |
| OPT-15 | 自动备份成功后才做保留清理，排除 Key/索引/日志 | 真用户目录权限、磁盘满与跨版本恢复实测 |

进程内自动化、合成测量和渲染结果不能代替真实设备。当前候选门禁见[报告](release/0.14.0-test-report.md)，不放宽性能预算，不修改历史成功/失败记录。
