# Windows 桌面宠物产品需求文档（PRD）

| 属性 | 当前基线 |
| :--- | :--- |
| 文档修订 | V2.8，2026-09-12，按 0.18.1 现有功能交互修订核对 |
| 软件 | 0.18.1 候选工作区；正式状态以本轮验证报告为准 |
| 平台 | Windows 桌面，WPF / .NET 8，win-x64 分发 |
| 状态 | 当前范围与验收约束；不代表所有验收已通过 |
| 事实来源 | src、tests、config、scripts、packaging 和 workflow；状态见 CURRENT_STATUS.md |

## 1. 产品定位与范围

桌宠是常驻入口，点击后进入紧凑工具窗，完成找文件、启动常用目标、维护待办和提醒。AI 是可选的结构化录入助手，不是手动功能的前置条件。产品按单机、单 Windows 账户运行，无登录、云同步或遥测上传实现。

当前页面为主页、待办、维护、设置。保留原需求编号用于追踪，但不再把待办和维护描述为占位页。下文“已接入”表示存在产品调用链，不代表实机验收已通过；“部分”表示已有基础而流程未齐；“候选”未进入当前用户能力。

### 1.1 当前能力边界

- 搜索只处理用户授权目录，不扫描全盘。名称和相对路径索引始终可用；用户可单独开启受控正文搜索，只读取不超过 128 KiB 的 `.txt`/`.md`，每范围最多 1,000 个文件和 16 MiB。结果支持固定、用户同意后的本地最近使用排序，以及选中后的打开、资源管理器定位、复制路径和加入快捷入口。应用入口从当前用户开始菜单及 App Paths 获取，不保证覆盖全部应用。
- AI 有多组命名配置、加密完整配置包、无 Key 兼容迁移、预设导入及确认写入的待办草稿。完整包迁移全部配置、当前项、自定义 Provider 和 Key，导入后立即启用；模型列表验证与主动确认后运行的生成验证分开。
- 提醒页面支持一次性/日历重复、多次时间、时区、仅此次或整条编辑、摘要/预览、AI 规则草稿及持久提醒中心。退出/关机期间不提供进程外准点提醒。
- 维护有模块预览、依赖成组备份、重启事务恢复及中断回滚、诊断和缓存清理；交互卸载可选清理已知个人数据。跨设备路径需重新定位，Key 不迁移。
- 全局快捷键默认从任意普通窗口打开搜索或快速新建待办；用户可停用或改键，冲突时整组回滚，托盘始终保留等价入口。
- 每日本地自动备份默认启用、24 小时最多一次并有保留上限；备份不含 Key、搜索索引和日志，失败不删除旧备份。
- 正常启动异步检查一次 GitHub Release；检查失败不阻止桌宠使用，也不清除此前已发现的可下载版本。设置可选运行期间周期检查、1～168 小时周期、系统代理及用户信任的 HTTPS 加速模板。
- 私有仓库可使用用户明确安全保存的只读 GitHub 令牌，输入框中未保存内容不得用于网络请求；令牌只保存在 Windows 凭据管理器。使用令牌时禁用第三方加速路由；安装器必须匹配 Release SHA-256 清单，且仅在用户确认后启动。
- RGS 四种 2D 预设已接入。受控纯文本正文检索已接入；PDF/Office、OCR、语义检索和外部宠物包导入未接入，通用聊天、可执行插件、云同步、3D 和角色商城不在产品范围。

### 1.2 隐私、数据及权限

Key 保存在 Windows Credential Manager，普通配置只存非敏感字段及凭据引用。查询词、完整路径、待办/AI 原文、鉴权头不写入默认日志。个人数据按当前账户保存，便携版不默认改为程序旁存储。

目录候选在确认前不枚举文件；正文读取需在设置中单独开启。停用正文搜索或撤销范围必须清除对应派生正文，过期任务不能恢复已撤销数据；正文、摘要和搜索词不进入日志、诊断、备份或 AI 请求。单项 AI 草稿只发送当前主动输入的文本、时间/时区提示及解析指令；今日安排只发送用户明确勾选的最多 12 项正文及时间/时区，未勾选事项只可提供不含标识或正文的匿名占用时间段用于避让。AI 写操作必须先展示草稿和明确目标，由用户确认后执行。

备份不含 Key，但含路径和待办等个人数据；诊断只导出白名单字段。任何新增外部服务、依赖、角色素材或数据上传都需独立评估，不能因写了 FeatureSettings 字段就启用。

### 1.3 质量目标与观察指标

性能预算来自 `config/performance-budgets.json`：20,000 条、200 次查询，搜索首屏 P95 ≤1 秒，窗口唤出 P95 ≤300 ms，冷启动 ≤3 秒；空闲 CPU ≤2%、工作集 ≤300 MB、拖动 ≥30 FPS、句柄 ≤1,200。预算是验收目标，不是已经达到的当前统计。

稳定性目标保持不少于 1,000 会话下无崩溃率 ≥99.5%；首次任务可用性目标保持 20 名目标用户五项核心任务各 ≥95%。尚无对应真实统计，不标记达标，也不因此加入遥测。用户研究和任何采集方案必须单独确认。

## 2. 信息架构与核心路径

| 入口 | 页面 / 结果 |
| :--- | :--- |
| 单击桌宠 | 唤出主页；搜索框获得焦点 |
| 主页 | 搜索授权、范围、关键词、类别、结果、快捷入口 |
| 待办 | 六种筛选、手动编辑、单项 AI 草稿、今日安排、提醒操作 |
| 维护 | 手动备份/恢复、每日自动备份、诊断、缓存清理 |
| 设置 | 角色、外观/动作、自启、全局快捷键、搜索范围/匹配、AI 配置、手册 |
| 托盘 | 桌宠/工具窗可见性、立即搜索、快速待办、设置、自启、帮助、提醒动作、退出 |

常驻与置顶相互独立；收起不是退出。一般桌宠唤出进入主页，托盘设置/提醒等定向入口按对应页面处理。再次打开不会创建第二套工具窗口。

## 3. 需求清单

### 3.1 桌宠与窗口

| ID | 优先级 | 状态与需求 |
| :--- | :--- | :--- |
| PET-01 | P0 | 已接入：透明桌宠常驻及点击入口，工具窗收起不结束桌宠 |
| PET-02 | P1 | 已接入：超过手势阈值才拖动，保存位置并按工作区纠偏；多屏实机待验 |
| PET-03 | P1 | 已接入：4 个预设、前向姿态、动作/气泡/低频漫游，可因交互中断 |
| WIN-01 | P0 | 已接入：单击切换，双击仅动作；Esc/关闭/外部点击收起 |
| WIN-02 | P0 | 已接入：宠物锚定，上方优先、空间不足改下方/缩短，避免遮挡角色 |
| WIN-03 | P0 | 已接入：独立可持久化常驻与置顶，选中不只靠颜色；常驻仍允许显式收起 |
| NAV-01 | P0 | 已接入：常规唤出进入主页，定向入口选择相应页面 |
| NAV-02 | P0 | 已接入：原两扩展位已经是待办和维护，与主页/设置共四页 |
| NAV-03 | P2 | 部分：页面结构可由开发修改，用户自定义页面配置未实现 |
| SET-01 | P0 | 已接入：设置是同一工具窗的一页；AI 未保存修改经确认保护；全局快捷键、自动备份和更新检查明确区分有修改与已保存，校验失败保留输入 |

### 3.2 搜索与快捷入口

| ID | 优先级 | 状态与需求 |
| :--- | :--- | :--- |
| SRCH-01 | P0 | 已接入：名称或相对路径片段，300 ms 输入延迟；通配符/正则需显式开启，正则异常提示 |
| SRCH-02 | P0 | 已接入：首次授权、范围添加/撤销、取消/重试/重建；范围单选含全部、仅应用、单目录 |
| SRCH-03 | P0 | 已接入：全部、文件夹、文档、应用、图片、视频、音频；筛选保留关键词 |
| SRCH-04 | P0 | 已接入：图标、名称、路径展示，双击或 Enter 打开，50 条分页 |
| SRCH-05 | P0 | 已接入：空态、加载、无结果、失效范围/目标与错误；陈旧请求不覆盖新结果 |
| SRCH-06 | P1 | 已接入：固定优先，可选最近使用；精确、前缀、包含后按修改时间和名称，显示匹配原因；自调权重未实现 |
| SRCH-07 | P1 | 已接入：正文搜索默认关闭，单独开启后只索引授权范围内受大小、数量、编码和重解析点限制的 `.txt`/`.md`；普通文字可命中正文并显示短摘要，停用/撤销即清除；OCR/语义未实现 |
| SRCH-08 | P0 | 已接入：选中结果后显示打开、资源管理器定位、复制路径和加入快捷入口；动作前重查目标，相同查询刷新按范围与路径恢复选择，查询条件变化清除旧选择 |
| SRCH-09 | P0 | 已接入：搜索词非空时显示清空入口，清空后移除旧选择并保持输入焦点；结果已存在于快捷入口时显示已加入状态并禁用重复提交 |
| QCK-01 | P0 | 已接入：主页六列网格、纵向滚动、空态添加入口 |
| QCK-02 | P0 | 已接入：文件/文件夹选择，默认系统图标及名称，重复提示，取消不创建 |
| QCK-03 | P0 | 已接入：编辑名称/描述及 ICO/PNG/JPG/JPEG 图标，失败提示和回退 |
| QCK-04 | P0 | 已接入：编辑、移除、上移/下移，持久化；移除不删除原文件 |
| QCK-05 | P0 | 已接入：启动失效提示、重新定位或移除，不自动删除 |
| QCK-06 | P1 | 已接入：批量拖入、分组/固定、多选、键盘/拖动排序、失效扫描取消、批量预览移除与单次撤销 |
| QCK-07 | P0 | 已接入：管理器显示筛选/选择数量，动作按选择数、固定区排序边界和扫描状态启用；Delete 只进入批量移除预览 |

### 3.3 系统、AI 与维护

| ID | 优先级 | 状态与需求 |
| :--- | :--- | :--- |
| TRAY-01 | P0 | 已接入：NotifyIcon 与 TaskbarCreated 恢复处理；Explorer 真重启待验 |
| TRAY-02 | P0 | 已接入：显示/隐藏桌宠、工具窗、设置、自启、帮助、退出；双击托盘恢复桌宠 |
| TRAY-03 | P0 | 已接入：退出取消后台工作、有限等待、释放托盘及资源 |
| HOTKEY-01 | P0 | 已接入：默认 `Ctrl+Alt+Space` 打开搜索、`Ctrl+Alt+T` 新建待办；设置可启停及改键，托盘有等价入口 |
| HOTKEY-02 | P0 | 已接入：只使用 `RegisterHotKey`/`WM_HOTKEY`；格式、重复项和占用校验，任一注册失败整组停用，退出释放 |
| AUTO-01 | P0 | 已接入：当前账户 HKCU Run，默认关闭，设置/托盘同步 |
| AUTO-02 | P0 | 已接入：写入后读实际状态、失败提示/回滚；重新登录实测单列 |
| HELP-01 | P0 | 已接入：系统默认程序打开包内离线 HTML |
| HELP-02 | P0 | 已接入：缺失/无关联/打开失败提示；没有在线兜底 |
| HELP-03 | P0 | 已接入：MD 与离线 HTML 覆盖当前四页、安装、退出和隐私 |
| AI-01 | P0 | 已接入：8 家供应商及 custom、模板、多组命名配置、当前项切换与删除 |
| AI-02 | P0 | 已接入：掩码 Key、Windows Credential Manager、普通配置仅存引用 |
| AI-03 | P0 | 已接入：GET models 校验 JSON 和模型存在，指纹控制保存；生成能力另需确认后验证，固定样例不写待办 |
| AI-04 | P0 | 已接入：必填/鉴权/权限/限流/端点/模型/响应/网络/超时/未知类别；取消、响应体预算和超时覆盖 |
| AI-05 | P1 | 已接入任务型草稿、重复规则/多次时间及确认；保留配置和自定义预设的无 Key 兼容迁移；通用聊天/自动文件操作未实现 |
| AI-06 | P0 | 已接入：用户以迁移口令加密导出全部 AI 配置、当前项、自定义 Provider 和 Key；导入先预览、确认后整体替换，Key 写入当前账户凭据库并立即启用；错误口令、篡改或中途写入失败不产生半导入状态 |
| DATA-01 | P0 | 已接入：设置 schema 5、待办 schema 4、提醒队列 schema 1、原子写入/previous/迁移备份；损坏读取后阻止空数据覆盖 |
| DATA-02 | P1 | 已接入：备份/恢复模块和差异预览、哈希/schema/容量/路径校验、重启事务/回滚、图标及队列依赖、真实日志根、离线清索引和卸载选择 |
| DATA-03 | P0 | 已接入：每日自动本地备份默认启用、24 小时节流、保留 1～30 份；手动立即创建/打开目录，失败保留已有备份，排除凭据、索引和日志 |
| UPDATE-01 | P0 | 已接入：每次正常启动异步检查一次稳定 GitHub Release；无更新、网络失败或元数据异常均不阻止启动，不自动下载；失败保留已发现版本 |
| UPDATE-02 | P1 | 已接入：设置可启停运行期间周期检查、配置 1～168 小时周期；使用 Windows 系统代理，可选仅 HTTPS 且恰含一个 `{url}` 的可信加速模板，失败回退官方地址 |
| UPDATE-03 | P0 | 已接入：私有仓库令牌明确保存后才进入 Windows 凭据管理器并用于请求，不进普通设置/日志/备份；带令牌只走 GitHub 官方/系统代理；安装器限制 200 MiB、原子下载并核对 Release `SHA256SUMS.txt`，用户确认后才启动 |

### 3.4 待办与提醒

| ID | 优先级 | 状态与需求 |
| :--- | :--- | :--- |
| TODO-01 | P1 | 已接入：待处理、今天、已逾期、未安排、未来 7 天、已完成；显示类型、时间和通道 |
| TODO-02 | P1 | 已接入：普通待办创建/编辑/完成/恢复/删除；独立提醒时间必填且不支持普通恢复 |
| TODO-03 | P1 | 已接入：一次性/每日/每周/工作日/自定义星期、间隔/结束/时区、额外时间、未来预览、仅此次/整条编辑与跳过/取消 |
| TODO-04 | P1 | 已接入：持久排队、静默/汇总、提醒中心完成/详情/5～60 分钟稍后；普通待办不自动完成，独立项最后一次提交后完成；退出不承诺准点 |
| TODO-05 | P1 | 已接入：自然语言单项操作、一次性/重复/多次时间，必要时澄清，与手动规则共用验证 |
| TODO-06 | P1 | 已接入：结构化预览、确认后写入、取消和最近一次撤销，重启不保留撤销历史 |
| TODO-07 | P1 | 已接入：修改/完成/删除/稍后意图，本地唯一目标校验，重名由用户选择 |
| TODO-08 | P1 | 已接入：失败保留输入，重试/手动降级；不发送已有列表、不落盘会话 |
| TODO-09 | P1 | 已接入：独立提醒逐项漫游/气泡开关，关闭后仍有托盘，系统动画限制漫游 |
| TODO-10 | P0 | 已接入：手动编辑器仅在已打开、存储可用且标题非空白时允许保存；日期、时间、重复和提醒校验继续在提交时执行 |
| TODAY-01 | P1 | 已接入：已逾期和未安排筛选；今天筛选同时识别截止、提醒和已确认计划开始时间 |
| TODAY-02 | P1 | 已接入：今日安排只列待处理事项，用户明确勾选 1～12 项；未勾选正文不进入请求，既有安排仅作为匿名时间段 |
| TODAY-03 | P1 | 已接入：AI 返回全部选中 ID 的今天未来时间块；绝对偏移、15 分钟～4 小时、跨日、新草稿互相重叠、既有安排冲突和额外 ID 均严格校验 |
| TODAY-04 | P1 | 已接入：无 AI 时使用完全本地的截止优先 30 分钟安排，从精确刻钟开始并自动跳过既有时间段；两种方式都先逐项预览，取消项不写入 |
| TODAY-05 | P1 | 已接入：`plannedStartAt` 与块结束截止时间整批原子保存；旧快照拒绝整批写入，最近一批可整体撤销，应用与撤销冲突分别提示 |

## 4. 关键边界与错误处理

所有页内选择需同步可见项及业务值，保留键盘选择、焦点轮廓和可访问名称。已保存 AI 配置切换先处理未保存草稿；测试期间编辑使结果失效，失败不覆盖旧配置。模型列表请求成功不能宣称已验证结构化任务能力。

范围重建失败/取消保留旧结果，撤销取消对应任务并删除名称、正文及偏好索引；watcher 恢复监听、子树更新和 Revision 分页有回归。正文全量重建使用暂存提交，通配符/正则只匹配元数据，元数据命中优先于仅正文命中。普通待办与独立提醒分开；先持久排队再推进时间，宿主接受不等于用户已读。重复规则保持所选时区钟点，静默跟随当前 Windows 时区；DST 缺失钟点顺延、重复钟点取较晚，漏触发汇总一次。备份验证后重启切换，失败回滚不能宣称全量成功。全局按键占用不得留下部分注册；自动备份未成功前不得执行保留清理。

## 5. 验收标准与追踪

以下是验收要求，不是通过清单；具体证据映射见 TEST_PLAN.md。沿用 AC 编号，避免历史测试追踪失联。

| 验收 ID | 对应需求 | 验收要求 |
| :--- | :--- | :--- |
| AC-01 | PET-01 | 运行观察至少 30 分钟，收起工具窗后宠物仍可点击 |
| AC-02 | PET-02/03、WIN-01/02 | 单击/双击/拖动互斥；主屏、边缘、副屏定位；前向姿态及空闲中断；唤出 P95 达预算 |
| AC-03 | NAV-01/02、WIN-03 | 四页各切换 20 次无重实例；常驻/置顶独立，重启保持，定向入口正确 |
| AC-04 | SRCH-01 | 授权深层名称和相对路径可查；通配符/正则开关生效且只处理元数据 |
| AC-05 | SRCH-02 | 未授权不枚举，范围隔离；移除后无缓存残留 |
| AC-06 | SRCH-03 | 各类别与未知类型样本正确过滤，关键词保持 |
| AC-07 | SRCH-04 | 双击和键盘分别打开文件、文件夹及应用 |
| AC-08 | SRCH-05 | 空输入/无结果/准备/无权限/失效/打开失败保留可操作反馈 |
| AC-09 | QCK-01 | 0、1、多项布局可用且不遮挡搜索主路径 |
| AC-10 | QCK-02 | 添加默认名/图标正确，取消不创建，重复明确处理 |
| AC-11 | QCK-03 | 图标/描述即时生效，无效图标有回退 |
| AC-12 | QCK-04 | 三项编辑/删除/重排后重启保持，原文件不变 |
| AC-13 | QCK-05 | 有效目标打开，失效目标保留且可定位/移除 |
| AC-14 | TRAY-01/02 | 隐藏后托盘恢复；菜单状态准确；Explorer 另行实测 |
| AC-15 | TRAY-03 | 索引中退出在 5 秒目标内完成，配置可读，无多余进程 |
| AC-16 | AUTO-01/02 | 两入口同步，重新登录验证开/关，拒绝时回滚 |
| AC-17 | HELP-01/02 | 3 秒内提交打开请求；缺失/无关联提示，不跳未知网址 |
| AC-18 | HELP-03 | 包内 HTML 可离线定位当前操作、安装和隐私内容 |
| AC-19 | SET-01 | 设置单实例，非法字段禁存，AI 未保存修改可保留/放弃/取消 |
| AC-20 | AI-01 | Key 掩码、模板不静默覆盖旧配置，保存项切换/删除正确 |
| AC-21 | AI-02 | Key 不进入普通存储/日志/快照，账户隔离及删除验证 |
| AC-22 | AI-03 | 未测/失败/编辑后禁存，重复请求受控，当前值测试成功才可保存；生成另验 |
| AC-23 | AI-04 | fake 服务覆盖错误类别，保留输入，不暴露响应/鉴权 |
| AC-24 | DATA-01 | 正常重启保持；写入中断/损坏可解释，备份不被破坏 |
| AC-25 | HOTKEY-01/02 | 两个默认组合键从其他应用定向唤出；改键/停用持久化；重复、占用和退出释放正确，托盘始终可达 |
| AC-26 | DATA-03 | 24 小时内不重复；1/7/30 份边界正确；压缩包含所需数据且无 Key/索引/日志；失败不删旧包 |
| AC-27 | AI-06 | 完整包密文不含可搜索的 Key/端点/名称；错误口令和篡改拒绝；多配置、活动项、Key 与预设跨数据目录恢复后即用；凭据失败回滚原配置 |
| AC-28 | UPDATE-01～03 | 启动只检查一次且失败不阻断、不清除已发现版本；周期和加速设置重启保持；只有已保存令牌可用于请求，令牌不进普通文件且不发送给加速服务；版本比较、资产名称、大小、HTTPS 和重定向受限；校验不符删除临时文件，校验通过后仍需用户确认安装 |
| AC-29 | TODAY-01～05 | 两个筛选正确；请求只含 1～12 条勾选事项正文及匿名占用时间；AI 和本地时间块 ID 完整、同日未来且避让既有安排；确认前文件不变，并发修改整批拒绝；成功写入可整批撤销且不覆盖后续编辑 |
| AC-30 | SRCH-07 | 默认不读正文；明确开启后只索引受控 `.txt`/`.md`，编码/大小/数量/总量/链接边界生效；元数据排在仅正文命中之前且摘要不超过 120 字符；停用、移除范围和维护清理均无正文残留，旧任务不能恢复数据 |
| AC-31 | SRCH-08 | 鼠标或键盘选择后四项操作可见且可达；打开/定位以单个安全路径参数调用系统，复制不持久化；失效目标禁用打开/定位/加入但仍可复制；同查询刷新恢复同一目标、条件变化清除旧选择 |
| AC-32 | SRCH-09/QCK-07/TODO-10/SET-01 | 清空搜索后焦点和条件连续；快捷入口重复状态实时更新；管理器无效动作、边界排序和扫描互斥正确；空白待办标题不可保存；三组显式保存设置只在有修改时写入且失败保留未保存状态 |

AC-NFR-01 对照性能预算；AC-NFR-02 为会话稳定性目标；AC-NFR-03 覆盖配置矩阵（含 175% 缩放、负坐标、多屏及系统转换）；AC-NFR-04 检查未授权/撤销与敏感日志；AC-NFR-05 为 20 名用户任务成功率。未收集证据不标记达标。

历史 AC-FUT-01～08 现作为待办能力的验收追踪，不再表示整项功能尚未实现：01 手动维护与重启；02 AI 草稿及明确时间；03 澄清和失败不写入；04 唯一目标及确认；05 一次性投递/稍后/完成；06 退出或休眠后的错过处理；07 未配置/失败手动降级；08 隐私、取消、撤销。重复/多次规则、提醒中心、AI 迁移、事务恢复和快捷管理的专项回归已加入；实机验收继续按扩展计划追踪。

## 6. 当前交付与后续范围

当前测试仅证明已执行的自动化套件，真实桌面仍需独立证据。版本候选、签名、硬件、打包和远端验证以当次报告为准；完整门禁未通过不得作为稳定版本发布。当前实施与剩余验收见[0.18.1－功能扩展计划](0.18.1－功能扩展计划.md)，现有功能完善状态见[已有功能优化](已有功能优化.md)。
