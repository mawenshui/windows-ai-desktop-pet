# 0.20.0 候选验证报告

日期：2026-09-13；状态：本地候选验证完成。本文只记录 0.20.0 当前工作区的当次证据，不把历史结果、合成渲染或静态检查当作真实桌面通过。

## 1. 范围与环境

- UPDATE-200-01～07 / UPDATE-01～04：内置更新线路、Release 安全边界、线路状态、设置减负、旧配置兼容。
- 回归 0.19.0 已有桌宠、搜索、快捷入口、待办/提醒、AI、维护、备份、快捷键和发布门禁反例。
- Windows `10.0.26200.0`，单物理显示器，主屏 2862×1440、100% 缩放、任务栏在底部。
- 打包 provenance：源提交 `0db177f6ea09d5bd2f785ca00aed9d3c7187b88f`，`sourceTreeState=dirty`，win-x64 self-contained，未签名。

## 2. 自动化与独立检查

| 检查 | 状态 | 当次结果 |
| :--- | :--- | :--- |
| `scripts/test.ps1 -CI` | PASS | 结构、676 个 UTF-8 文本、发布证据反例、Release 全解决方案构建；317/317 xUnit，0 跳过，构建 0 警告、0 错误 |
| 更新专项回归 | PASS | 20/20；官方/加速顺序、单条与全部失败、匿名请求、固定仓库/标签/资产、同版本非稳定元数据、大小、重定向、digest/清单、临时文件、VM 状态和旧模板清理 |
| 进程内 WPF 生命周期与渲染 | PASS | 17/17；设置外壳、控件状态与匿名快照可渲染；不替代真实输入 |
| `scripts/test-ui-e2e.ps1 -CI -NoBuild` | FAIL | 宿主不能把应用设为前台；在 `inline-search-scope`、发送业务输入之前停止，未生成本轮桌面截图 |
| `scripts/test-performance.ps1 -Enforce` | FAIL | 匿名 20,000 条名称搜索、200 次第一页查询完成：P50 3.8149 ms、P95 4.497 ms、max 5.264 ms；真实 UI 输入因相同前台限制未发送，所以整体 gate 失败 |
| `scripts/test-system-e2e.ps1 -CI` | PASS / SKIP | 8 项几何合同及启动/帮助/存储/退出 smoke PASS；当前 100% 缩放和底部任务栏 PASS；Explorer 重启、多屏/负坐标、125%～200%、热插拔、休眠、时区为 SKIP |
| `scripts/package.ps1` | PASS | 生成 0.20.0 便携版、安装版、SHA-256 清单和 dirty provenance |
| `scripts/smoke-release.ps1 -Version 0.20.0` | PASS / SKIP | 便携运行、静默安装、安装后启动、静默卸载及安装路径自启清理 PASS；无受保护签名证书，Authenticode SKIP |

## 3. 候选资产

| 文件 | 大小 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.20.0-portable.zip` | 76,789,918 字节 | `2acf69596dec57ddf0923a49e5f9ba62f43d3d98e2909092acf8c2b06540b5b7` |
| `windows-ai-desktop-pet-v0.20.0-setup.exe` | 53,974,558 字节 | `c0ba4577217c17d1930fa900da7a2dbd0b045c07b6bb7ee5eb76f6b47ee95de1` |

清单为 `dist/checksums/SHA256SUMS.txt`，provenance 为 `dist/checksums/RELEASE_PROVENANCE.json`。package-smoke 使用上述当前资产；安装器和应用没有 Authenticode 签名。

## 4. 线路点时核验

2026-09-13 使用匿名公开 GitHub 原始文件探测三条内置下载线路，均返回 HTTP 200。对公开 GitHub latest Release API 探测时，`gh-proxy.com` 返回 200 JSON，`ghfast.top` 与 `ghproxy.net` 返回 403，因此后二者只进入下载路由。此结果只证明当时可达，不承诺长期可用，也不进入离线 CI 的稳定断言。

## 5. 结论

0.20.0 的设计、实现、文档、版本、完整自动化、系统 smoke、打包和 package-smoke 已完成；功能可作为本地候选检查。由于真实前台 UI 与强制性能 gate 为 FAIL、签名和多数物理环境为 SKIP、工作区 dirty，且未提交、推送或验证远端资产，**不得创建稳定 `v0.20.0` 标签或 GitHub Release**。应在可前台激活的解锁桌面和具备签名/物理条件的发布环境重新取证，再从干净提交重新打包。
