# 0.23.0 候选验证报告

日期：2026-09-14；状态：本地候选验证完成，稳定发布门禁未通过。本文只记录 0.23.0 当前工作区的当次证据，不把历史结果、进程内渲染或静态检查当作真实桌面通过。

## 1. 范围与环境

- UX-230-01～18 / TODO-19 / AC-38。
- 回归既有桌宠、搜索、快捷入口、待办/提醒、AI、维护、更新、打包和发布门禁反例。
- Windows `10.0.26200.0`，.NET 运行时 `10.0.11`；当前系统报告一个物理显示器、主屏 100% 缩放和底部任务栏。
- 打包 provenance 记录源提交 `def624d2705585743c46f05290191c5120410bf0`、`sourceTreeState=dirty`、`signed=false`；独立证据输入指纹为 `b7e14c47017d0d1384dfb1c94c4dff91092e78efe255d875caa65384a3e61ffe`。它是候选证据，不满足正式发布的干净源要求。

## 2. 自动化与独立检查

| 检查 | 状态 | 当次结果 |
| :--- | :--- | :--- |
| 变更前基线 | PASS | `scripts/test.ps1 -CI`：334/334，确认 0.22.0 工作区起点可回归 |
| WPF 生命周期定向回归 | PASS | `PetToolWindowLifecycleTests`：19/19，提醒页面动态资源与窗口构造通过 |
| 第一轮 `scripts/test.ps1 -CI` | PASS | 结构/UTF-8、发布证据反例、Release 构建和 338/338 xUnit 全部通过；0 警告、0 错误 |
| 文档后完整回归 / automated evidence | PASS | 两轮完整回归及正式 evidence 均为 338/338，0 跳过、0 警告、0 错误；项目校验和门禁反例通过 |
| `scripts/test-ui-e2e.ps1 -CI -NoBuild` | FAIL | `inline-search-scope` 阶段无法把应用置于前台；runner 在发送业务输入前停止，没有用属性赋值伪造真实鼠标/键盘结果 |
| `scripts/test-performance.ps1 -Enforce -NoBuild` | FAIL | 交互桌面无法把应用置前，性能输入未发送且未生成完整报告；整组不判 PASS |
| `scripts/test-system-e2e.ps1 -CI` | PASS / SKIP | 8/8 几何合同和应用 `--smoke` PASS；资产、宠物、搜索、快捷入口、AI Provider、安全存储、自启、帮助和退出通过；当前 100% 主屏/底部任务栏通过，其余物理项 SKIP |
| `scripts/package.ps1` | PASS | 已生成便携 ZIP、Inno Setup 安装器、SHA-256 清单和 dirty/unsigned provenance |
| `scripts/smoke-release.ps1 -Version 0.23.0` | PASS / SKIP | 便携运行、静默安装、安装后运行、静默卸载和安装位置自启清理通过；未配置证书，脚本内 Authenticode 检查 SKIP |
| signatures evidence | SKIP | 便携包内主程序与安装器均为 `NotSigned` |
| hardware evidence | SKIP | 多屏、100%～200% 全矩阵、热插拔、休眠/恢复、时区/时钟、Explorer 重启和覆盖升级需要见证实机/一次性账户环境 |
| `scripts/verify-release.ps1` | FAIL | 首先拒绝 dirty 工作区；即使清理工作区，UI/performance/signatures/hardware 仍含 FAIL/SKIP |

## 3. 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.23.0-portable.zip` | 76,822,631 | `53db4735577cec8d2322c8366150ad95babf4360ea7f33a354d98ec852f4c8d2` |
| `windows-ai-desktop-pet-v0.23.0-setup.exe` | 54,004,779 | `db63b8cfc883a29cda832497c8f9f8601e4208b0e0e6ca43f93c0eab169bfb0c` |

清单为 `dist/checksums/SHA256SUMS.txt`，provenance 为 `dist/checksums/RELEASE_PROVENANCE.json`。两项哈希与本轮 package-smoke、signatures 和 hardware 证据绑定一致，但候选来自 dirty 工作区且未签名。工作区中用户原有的 0.19.0/0.20.0/0.22.0 未跟踪候选资产未被删除或改名。

## 4. 结论

UX-230-01～18 已按设计顺序实现，PRD、工程规范、技术设计、手册、测试计划、设计记忆和版本资料已同步，自动化回归、候选打包、系统及分发烟雾完成；但真实前台 UI、完整性能、签名和物理环境门禁存在 FAIL/SKIP，且资产不是从干净提交构建。因此本版只能保留为本地候选，**不得创建稳定 `v0.23.0` 标签或 GitHub Release**。修复环境后必须从干净提交重新打包，并完整重跑 UI、性能、签名和硬件证据。
