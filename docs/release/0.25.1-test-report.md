# 0.25.1 验证报告

日期：2026-09-18  
版本：0.25.1

## 当前结论

版本与文档同步后的完整自动化回归为 371/371 通过，Release 构建 0 警告、0 错误。打包与独立发布门禁仍在执行；下列状态只按真实结果更新，未执行项不计为 PASS。

## 针对性验证

| 检查 | 状态 | 结果 |
| :--- | :--- | :--- |
| 更新代理处理器 | PASS | 默认处理器显式使用 `HttpClient.DefaultProxy`；检查与下载复用同一客户端，注入 fake HTTP 的单元测试不访问真实网络 |
| 更新线路公开样本 | PASS | 2026-09-18 匿名只读请求中，GitHub 官方 API 与 `gh-proxy.com` 对公开 `cli/cli` latest Release 均返回 HTTP 200 |
| 当前更新源匿名访问 | FAIL | 同一请求对 Private `mawenshui/windows-ai-desktop-pet` 返回 HTTP 404；必须由维护者明确公开仓库后重验，代码无法绕过权限 |
| 更新失败分类 | PASS | 网络不可用、更新源匿名 404、不安全元数据分开映射；失败保留已发现版本 |
| 主题往返 | PASS | WPF STA 回归验证深色主体/卡片/文字同步变化，并验证切回浅色完整恢复；语义状态色和阴影使用同一动态调色板 |

## 发布门禁

| Gate | 状态 | 说明 |
| :--- | :--- | :--- |
| automated | PASS | `scripts/test.ps1 -CI`：371/371、0 跳过；Release 构建 0 警告、0 错误；结构、UTF-8 与发布证据反例验证通过 |
| ui | 未执行 | 需独立交互桌面验证真实主题切换及既有主路径 |
| system | 未执行 | 需按 0.25.1 当前提交收集 |
| performance | 未执行 | 需按 0.25.1 当前提交收集 |
| package-smoke | 未执行 | 尚未生成 0.25.1 正式候选资产 |
| signatures | 未执行 | 按实际证书环境记录 |
| hardware | 未执行 | 按物理显示器、缩放、休眠与升级环境记录 |

当前不能创建 `v0.25.1` 标签或 GitHub Release，也不创建任何预发布版本。
