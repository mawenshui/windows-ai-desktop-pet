# 0.25.1 验证报告

日期：2026-09-18  
版本：0.25.1

## 当前结论

版本与文档同步后的完整自动化回归为 371/371 通过，Release 构建 0 警告、0 错误。system、package-smoke 与仓库公开后的匿名真实更新链路为 PASS；UI、performance 为 FAIL，signatures 与 hardware 为 SKIP，因此不满足正式发布门禁。

## 针对性验证

| 检查 | 状态 | 结果 |
| :--- | :--- | :--- |
| 更新代理处理器 | PASS | 默认处理器显式使用 `HttpClient.DefaultProxy`；检查与下载复用同一客户端，注入 fake HTTP 的单元测试不访问真实网络 |
| 当前更新源匿名元数据 | PASS | 2026-09-18 确认仓库为 Public；GitHub 官方 latest API 与 `gh-proxy.com` 对 `mawenshui/windows-ai-desktop-pet` 均返回 HTTP 200，当前 Latest 为普通正式版 `v0.24.0` |
| 当前更新源匿名下载 | PASS | 项目更新客户端匿名发现 `v0.24.0`，经内置“智能加速线路”下载完整安装器并通过 SHA-256 校验；实测哈希为 `a1551cb78e6aef3df63e88b1ee7999f82c4c925f25cb19f3a5fbc74c825c1c84` |
| 更新失败分类 | PASS | 网络不可用、更新源匿名 404、不安全元数据分开映射；失败保留已发现版本 |
| 主题往返 | PASS | WPF STA 回归验证深色主体/卡片/文字同步变化，并验证切回浅色完整恢复；语义状态色和阴影使用同一动态调色板 |

## 发布门禁

| Gate | 状态 | 说明 |
| :--- | :--- | :--- |
| automated | PASS | `scripts/test.ps1 -CI`：371/371、0 跳过；Release 构建 0 警告、0 错误；结构、UTF-8 与发布证据反例验证通过 |
| ui | FAIL | 当前 Codex 交互桌面无法将应用置前；runner 在 `inline-search-scope` 阶段发送输入前安全停止。进程内 WPF 主题往返测试 PASS，但不替代真实输入 gate |
| system | PASS | 8/8 几何合同及启动/帮助/存储/退出 smoke 通过；当前为单屏 100% 和底部任务栏，其他物理条件单列 SKIP |
| performance | FAIL | 强制脚本在真实前台采样阶段连续两分多钟没有产生可信样本，人工终止；未降低预算，也未以非交互指标代替完整 gate |
| package-smoke | PASS | clean source commit 的便携启动、安装、Windows 凭据存储、安装后启动、卸载及自启清理通过 |
| signatures | SKIP | 未配置受保护的 Authenticode 证书 |
| hardware | SKIP | 当前只有单屏 100%；多屏/负坐标、125%～200%、热插拔、休眠/唤醒、时区改时、Explorer 重启和覆盖升级未实测 |

## 构建资产

来源提交：`af066a7a9f2914c79a13c574491dfcf39a586cb9`，provenance `sourceTreeState=clean`，`signed=false`。

| 资产 | 大小 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.25.1-portable.zip` | 76,883,982 | `cd7e696068b64233b74675c2694cf78e5ebb6c1bceb769616e4eb78537fe6e01` |
| `windows-ai-desktop-pet-v0.25.1-setup.exe` | 54,045,632 | `2bf98ef7f8ba134f703857a4738c7228dc0d224d4f17de0cdd93e441b7620ed9` |

当前不能创建 `v0.25.1` 标签或 GitHub Release，也不创建任何预发布版本。匿名更新阻塞已经解除；仍需取得 UI、performance、signatures 与 hardware 的当次 PASS 证据后，才能创建普通正式 Release。
