# 0.16.0 候选验证报告

日期：2026-09-09。对象：0.16.0 候选工作区。本文只记录本轮最终代码的当次结果；0.15.0 及更早报告不作为本轮证据。

## 自动化

- 今日安排定向测试：**PASS，9/9**。覆盖精确 ID/非重叠结果、额外 ID 与重叠拒绝、本地顺序安排、只发送勾选内容、原子批量更新、旧快照整批拒绝、整批撤销、六种筛选、schema 3→4 迁移及无结束时间时拒绝计划开始时间。
- Release 解决方案构建：**PASS，0 警告、0 错误**。
- `scripts/test.ps1 -CI`：**PASS，278/278**；项目结构、UTF-8、密钥文件名、Release 证据反例、还原、构建与完整 xUnit 均通过。

## 分发与环境门禁

| Gate | 当前结果 | 说明 |
| :--- | :--- | :--- |
| automated | **PASS** | `scripts/test.ps1 -CI`，278/278；Release 构建 0 警告、0 错误 |
| package-smoke | 待执行 | 0.16.0 资产尚未从已提交源码构建 |
| system | **PASS / SKIP** | 8/8 几何契约、应用启动/帮助/存储/退出、凭据库、当前 100% 主屏与底部任务栏 PASS；Explorer 重启及需改变设备/系统状态的项目 SKIP |
| ui | **FAIL** | `inline-search-scope` 无法把应用置前，脚本在第一次物理输入前停止；未向锁屏或其他窗口发送输入，今日安排真实选择/草稿/确认/撤销未执行 |
| performance | **FAIL** | 匿名 20,000 项搜索 200 次 PASS（p50 3.7239 ms、p95 4.3318 ms、max 4.7114 ms）；窗口无法置前，交互性能输入未发送 |
| signatures | 待执行 | 按受保护签名证书实际状态记录 |
| hardware | **SKIP** | 当前仅一台 1920×1080、100% 显示器；负坐标、多屏/热插拔、125%～200%、休眠/恢复、时区/时钟和真实升级没有对应物理条件 |

任何待执行、FAIL 或 SKIP 都表示完整发布门禁未通过。当前 UI、性能和物理门禁没有完整证据，因此不得创建 `v0.16.0` 标签或 GitHub Release。

## 隐私与数据验证边界

- 自动化使用匿名待办和 fake AI，不读取真实待办、凭据或用户目录。
- 定向测试证明构造的 AI 请求只有选中项；真实服务的网络出站仍需代理/服务端见证。
- `plannedStartAt`、批量更新时间戳和撤销仅涉及待办本地数据；不会写入 API Key 或 AI 响应。

## 候选资产

0.16.0 便携版、安装器、SHA-256 与 provenance 将在实现提交后从干净源码重建，并在此记录字节数、哈希、源提交、输入状态与烟雾结果。
