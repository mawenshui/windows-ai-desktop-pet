# 0.21.0 候选验证报告

日期：2026-09-13；状态：本地候选验证完成，稳定发布门禁未通过。本文只记录 0.21.0 当前工作区的当次证据，不把历史结果、合成渲染或静态检查当作真实桌面通过。

## 1. 范围与环境

- UX-210-01～19 / NAV-05 / SRCH-11 / QCK-09 / TODO-12～14 / TODAY-06 / DATA-05。
- 回归既有桌宠、搜索、快捷入口、待办/提醒、AI、维护、更新、打包和发布门禁反例。
- Windows `10.0.26200.0`，单物理显示器，2862×1440，主屏 100% 缩放，任务栏位于底部。
- 打包 provenance 记录源提交 `0db177f6ea09d5bd2f785ca00aed9d3c7187b88f`、`sourceTreeState=dirty`、`signed=false`；它是候选证据，不满足正式发布的干净源要求。

## 2. 自动化与独立检查

| 检查 | 状态 | 当次结果 |
| :--- | :--- | :--- |
| 定向 xUnit | PASS | 327/327，0 跳过；新增搜索重置/计数、编辑保护、统一撤销/并发拒绝、今日安排批量选择、维护状态和 Esc 层级 |
| `scripts/test.ps1 -CI` | PASS | 结构/UTF-8（最终校验 699 个文本文件）、发布证据反例、Release 构建和 327/327 xUnit 全部通过；0 警告、0 错误 |
| `scripts/test-ui-e2e.ps1 -CI -NoBuild` | FAIL | `inline-search-scope` 阶段无法把应用置于前台；runner 在发送业务输入前停止，未伪造交互结果 |
| `scripts/test-performance.ps1 -Enforce` | FAIL | Release 构建通过；当前交互桌面无法把应用置于前台，性能输入未发送，因此未产生可用于本版验收的真实 UI 指标 |
| `scripts/test-system-e2e.ps1 -CI` | FAIL / SKIP | 8/8 几何合同、当前 100% 主屏和底部任务栏 PASS；启动 smoke 的资产、宠物、搜索、快捷入口、AI Provider、自启和帮助 PASS，但安全存储因当前登录会话不可用而 FAIL；Explorer 重启、物理多屏/负坐标、125%～200%、热插拔、休眠和时区变更 SKIP |
| `scripts/package.ps1` | PASS | 已生成便携 ZIP、Inno Setup 安装器、SHA-256 清单和 dirty/unsigned provenance |
| `scripts/smoke-release.ps1 -Version 0.21.0` | FAIL / SKIP | Authenticode 因未配置受保护证书而 SKIP；便携 `--smoke` 在安全存储检查处返回 2，安装、安装后启动及卸载阶段未执行 |

## 3. 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.21.0-portable.zip` | 76,799,872 | `2d68b6927d62fa702bb42a85572565978c3c95a31bc1ec9cc69373576650cd48` |
| `windows-ai-desktop-pet-v0.21.0-setup.exe` | 53,982,332 | `1e1bbde362492da7873bf4921969802a1c0630b049835e65d7037d02f5122ca4` |

清单为 `dist/checksums/SHA256SUMS.txt`，provenance 为 `dist/checksums/RELEASE_PROVENANCE.json`。两项哈希与本轮打包输出一致，但候选来自 dirty 工作区且未签名。

## 4. 结论

UX-210-01～19 的实现与自动化回归完成，候选资产已生成；但真实前台 UI、强制性能、安全存储、便携启动、签名和物理环境门禁存在 FAIL/SKIP，且资产不是从干净提交构建。因此本版只能保留为本地候选，**不得创建稳定 `v0.21.0` 标签或 GitHub Release**。修复环境后必须从干净提交重新打包，并完整重跑 UI、性能、系统、package-smoke、签名和硬件证据。
