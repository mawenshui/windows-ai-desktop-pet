# 0.14.0 候选验证报告

日期：2026-09-08。对象：0.14.0 候选工作区。本文只记录本轮实际执行结果；0.13.0 及更早报告不作为本轮证据。

## 自动化

- `scripts/test.ps1 -CI`：**PASS**。项目结构校验、发布证据反例、Release 全解决方案构建和 xUnit 均通过；构建为 0 警告、0 错误，xUnit **249/249**。
- `scripts/validate-project.ps1 -CI`：**PASS**。
- 新增回归覆盖：全局快捷键格式规范化、非法/重复拒绝；快速待办定向和标题焦点；设置 schema 4 迁移；自动备份 24 小时节流、保留上限、模块排除、失败保留旧包。

## 分发与环境门禁

| Gate | 当前结果 | 说明 |
| :--- | :--- | :--- |
| automated | PASS | `scripts/test.ps1 -CI` 完整通过，249/249 |
| package-smoke | PASS | 自包含便携版、安装、已安装启动、卸载和自启清理在正常 Windows 会话通过；沙箱登录会话失败不作为通过证据 |
| system | PASS / SKIP | 8/8 几何合同、启动/帮助/存储/退出、当前 100% 主屏和底部任务栏通过；其余物理条件按下表 SKIP |
| ui | **FAIL** | `inline-search-scope`：当前交互桌面不能把应用置前，runner 停止且未发送输入；无本轮真实鼠标/键盘通过证据 |
| performance | **FAIL** | 20,000 条匿名索引搜索 p95 为 4.6504 ms；应用窗口已出现，但 UI Automation 无法定位“小方工具袋”控件，强制门禁失败 |
| signatures | SKIP | 本轮未提供代码签名证书；两个候选资产均为 unsigned |
| hardware | SKIP | Explorer 重启、多屏/负坐标、125%～200% 缩放、热插拔、休眠/唤醒、时区/时钟和真实升级未具备本轮实机条件 |

任何待执行、FAIL 或 SKIP 都表示完整发布门禁未通过。当前不得创建 `v0.14.0` 标签或 GitHub Release。

## 系统与新增功能边界

- `scripts/test-system-e2e.ps1`：**PASS（可自动执行部分）**。系统为 Windows NT 10.0.26200.0，单显示器 1920×1080、主屏 100% 缩放；Explorer 重启与需要改变设备/系统状态的场景均明确 SKIP。
- EXT-09 的解析、重复拒绝、设置持久化和定向焦点有自动化证据。当前会话直接探测两个默认组合均可由 Windows 注册；但隔离候选进程收到外部合成按键后未显示工具窗，因此系统回调烟雾为环境受限 **FAIL**。物理键盘、实际占用冲突和正常退出释放仍为 **SKIP**。
- EXT-10 的创建、24 小时节流、保留上限、敏感/派生模块排除和失败保留旧包有自动化证据；真用户目录权限、磁盘满和跨版本恢复仍为 **SKIP**。

## 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.14.0-portable.zip` | 76,694,682 | `07969334239cabb7b3d3be275796809a355ec1cba37f8a7b64047402115a9220` |
| `windows-ai-desktop-pet-v0.14.0-setup.exe` | 53,903,441 | `2807169d1708827b7b892ac5cbc21a4e7d7c1694bbc89f82a11323e4e31afbf7` |

入口：[便携版](../../dist/portable/windows-ai-desktop-pet-v0.14.0-portable.zip)、[安装版](../../dist/installer/windows-ai-desktop-pet-v0.14.0-setup.exe)、[SHA256SUMS](../../dist/checksums/SHA256SUMS.txt)、[构建记录](../../dist/checksums/RELEASE_PROVENANCE.json)。候选包从源码提交 `57e90f0` 完整清理 Release 输出后构建，程序集 ProductVersion 为 `0.14.0+57e90f0151938c59c5381a8666210d40ad26ecc3`。构建记录因生成 dist 后读取工作区而如实标记 `sourceTreeState: dirty`，并标记 `signed: false`；这些是可复核候选资产，不是正式 Release。
