# 0.24.0 候选验证报告

日期：2026-09-15；状态：本地候选验证完成，稳定发布门禁未通过。本文只记录 0.24.0 当前工作区证据，不复用历史版本结果。

## 1. 范围

- JOURNAL-01～08 / AC-JOURNAL-01～06：清单/复盘二级视图、本地投影、自动保存、revision、跨日快照、历史浏览/编辑/删除、Markdown 导出、维护集成和无障碍语义。
- 回归既有桌宠、搜索、快捷入口、待办/提醒、AI、维护、更新、构建和发布证据反例。
- 明确排除任何 Codex/Claude 等 Agent 状态指示、Hooks 和状态文件读取。

## 2. 自动化与独立检查

| 检查 | 状态 | 当次结果 |
| :--- | :--- | :--- |
| 变更前基线 | PASS | 0.23.0 工作区 `scripts/test.ps1 -CI`：338/338，0 警告、0 错误 |
| 复盘定向回归 | PASS | 存储、revision、损坏保护、32 MiB/字段限制、投影、跨日冻结、Markdown、ViewModel、备份恢复、自动备份和 WPF 合同测试通过 |
| 完整 `scripts/test.ps1 -CI` | PASS | 项目/UTF-8/发布证据反例校验通过；Release 构建 0 警告、0 错误；349/349 xUnit 通过，0 跳过 |
| `scripts/package.ps1` | PASS | 生成便携 ZIP、Inno Setup 安装器、SHA-256 清单和 provenance；同时修复 Windows PowerShell 5.1 的相对路径、Git ignore、原子替换兼容性 |
| 资产哈希复核 | PASS | 对两个候选资产重新执行 SHA-256，结果与 `SHA256SUMS.txt` 一致 |
| `scripts/smoke-release.ps1 -Version 0.24.0` | PASS / SKIP | 正常 Windows 用户会话中便携运行、静默安装、安装后运行、静默卸载及自启清理通过；未配置签名证书，Authenticode SKIP |
| `scripts/test-system-e2e.ps1 -CI` | PASS / SKIP | 8/8 几何合同与应用 smoke 通过；当前单屏、100% 缩放、底部任务栏，非当前物理条件与 Explorer/热插拔/休眠/改时为 SKIP |
| 前台 UI / performance | NOT RUN | 当前 runner 未覆盖复盘的真实鼠标、键盘、保存/覆盖对话框和 200% 视觉验收，不以 WPF 合同替代 |
| signatures / hardware | SKIP | provenance 为 `signed=false`；物理多屏、125%～200%、热插拔、休眠、改时/时区和 Explorer 重启缺少对应环境 |
| `scripts/verify-release.ps1` | BLOCKED（未执行） | 工作区为 dirty，且 UI/performance/signatures/hardware 门禁未齐；不满足稳定发布前置条件 |

## 3. 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.24.0-portable.zip` | 74,618,065 | `47e7dbd7cc75f12a004cead8ace67838281a47cd2a696557bc4cbc2d9de11ab4` |
| `windows-ai-desktop-pet-v0.24.0-setup.exe` | 54,018,577 | `248b14299835b1c68409b160c8a289df4e6af8430553e35dc5f1200cf415089c` |

清单为 `dist/checksums/SHA256SUMS.txt`，provenance 为 `dist/checksums/RELEASE_PROVENANCE.json`，记录源提交 `def624d2705585743c46f05290191c5120410bf0`、`sourceTreeState=dirty`、`signed=false`。首次受限沙箱烟测因 DPAPI 登录会话不可用而失败；在获准的正常用户会话复跑后全部产品路径通过，故不把该宿主隔离错误记为产品失败。

## 4. 结论

JOURNAL-01～08 已按文档逐项实现并完成自动化、候选打包、系统边界和分发烟测；但真实复盘前台交互、性能、签名、完整物理矩阵、干净提交、推送及远端资产仍未完成。因此 0.24.0 只能保留为本地候选，**不得创建稳定 `v0.24.0` 标签或 GitHub Release**。
