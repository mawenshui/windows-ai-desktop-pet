# 0.18.0 候选验证报告

日期：2026-09-12。版本：0.18.0。打包源提交：`ca0683249486689f91d6b517fea5c01a6349b77b`；输入指纹：`bdcc0985123ef069ef331c1e41c461d1bd317d506ed8d92fc3a0c8f8dd0d51d9`。环境为 Microsoft Windows NT 10.0.26200.0，证据驱动运行时 10.0.11。历史版本结果未作为本轮证据。

## 发布门禁

| 门禁 | 结果 | 证据 |
| :--- | :--- | :--- |
| automated | PASS | 项目/UTF-8 校验、发布门禁反例、Release 构建、297/297 xUnit |
| ui | FAIL | 应用无法成为前台窗口；在 `inline-search-scope` 输入前停止，未发送鼠标或键盘事件 |
| system | PASS | 多屏/缩放几何合同及启动、帮助、存储、退出 smoke 通过 |
| performance | FAIL | 应用无法成为前台窗口；真实输入前停止，未形成可接受的窗口性能报告 |
| package-smoke | PASS | 便携运行、静默安装、安装后运行、静默卸载及本安装位置自启清理通过 |
| signatures | SKIP | 安装器和便携主程序均为 `NotSigned`，当前未配置受保护签名证书 |
| hardware | SKIP | 多屏、缩放矩阵、热插拔、休眠、时区/时钟、Explorer 重启和升级保留缺少见证环境 |

完整 CI 的 Release 构建为 0 警告、0 错误。搜索结果动作的中文、空格和特殊字符路径单参数传递、系统失败隔离、失效命令状态、复制路径、快捷入口去重、刷新选择恢复、条件变化清除及 WPF AutomationId 均有自动化覆盖。独立 UI 脚本还加入了真实选择结果、复制精确路径及创建快捷入口步骤，但本环境在更早的前台检查处停止，因此这些步骤没有被误报为 PASS。

system 和 package-smoke 最初在受限进程中因 Windows 凭据登录会话不可用而失败；按环境隔离规则在当前本机用户会话重跑后均为 PASS，最终证据文件只保留后一次结果。UI 和 performance 在本机交互权限下重跑仍无法置前，继续记录 FAIL。

## 候选资产

| 资产 | 大小 | SHA-256 | package-smoke |
| :--- | ---: | :--- | :--- |
| `windows-ai-desktop-pet-v0.18.0-portable.zip` | 76,779,487 bytes | `ba4cfbc08be54663cd02690cd93a1c8fd8852391c8845f34df129c8128f8d49d` | PASS |
| `windows-ai-desktop-pet-v0.18.0-setup.exe` | 53,962,781 bytes | `e4dff40526389ec86eb9f30532191c47c6625f44e2eca2cf34f8f43e78f9b015` | PASS |

`dist/checksums/SHA256SUMS.txt` 只包含上述两个当前资产；`RELEASE_PROVENANCE.json` 记录版本 0.18.0、干净源提交、win-x64 self-contained、构建时间和未签名状态。

## 结论

实现、自动化、system、打包和 package-smoke 已完成；独立 UI 与性能仍为 FAIL，签名与硬件仍为 SKIP。该证据集不能通过 `scripts/verify-release.ps1`，因此不得把 0.18.0 标记为稳定 GitHub Release，也不创建 `v0.18.0` 标签。候选资产可用于后续在可置前的交互桌面、签名环境和见证实机上补齐同字节验证。
