# 0.22.0 候选验证报告

日期：2026-09-14；状态：本地候选验证完成，稳定发布门禁未通过。本文只记录 0.22.0 当前工作区的当次证据，不把历史结果、进程内渲染或静态检查当作真实桌面通过。

## 1. 范围与环境

- UX-220-01～19 / TODO-15～18 / AC-35～37。
- 回归既有桌宠、搜索、快捷入口、待办/提醒、AI、维护、更新、打包和发布门禁反例。
- Windows `10.0.26200.0`，.NET 运行时 `10.0.11`；单物理显示器，2862×1440，主屏 100% 缩放，任务栏位于底部。
- 打包 provenance 记录源提交 `def624d2705585743c46f05290191c5120410bf0`、`sourceTreeState=dirty`、`signed=false`；输入指纹为 `93551cb2710f518af2666ee4cba7f0a3101f67dd0125c1a3648034a9bee662b4`。它是候选证据，不满足正式发布的干净源要求。

## 2. 自动化与独立检查

| 检查 | 状态 | 当次结果 |
| :--- | :--- | :--- |
| 变更前基线 | PASS | `scripts/test.ps1 -CI`：327/327，确认 0.21.0 工作区起点可回归 |
| 定向与首轮回归 | PASS | 待办相关 49/49；随后完整 334/334，0 跳过、0 警告、0 错误 |
| `scripts/test.ps1 -CI` / automated evidence | PASS | 结构/UTF-8、发布证据反例、Release 构建和 334/334 xUnit 全部通过；0 警告、0 错误 |
| `scripts/test-ui-e2e.ps1 -CI -NoBuild` | FAIL | `inline-search-scope` 阶段无法把应用置于前台；runner 在发送业务输入前停止，未伪造鼠标/键盘结果 |
| `scripts/test-performance.ps1 -Enforce -NoBuild` | FAIL | 匿名 20,000 条名称查询 200 次，首屏 P50 3.7021 ms、P95 4.2344 ms、最大 5.1454 ms，低于 1,000 ms 搜索预算；但交互桌面无法把应用置前，UI 性能输入未发送，整组不判 PASS |
| `scripts/test-system-e2e.ps1 -CI` | FAIL / SKIP | 8/8 几何合同、当前 100% 主屏和底部任务栏 PASS；启动 smoke 的资产、宠物、搜索、快捷入口、AI Provider、自启和帮助 PASS，但安全存储因当前登录会话不存在而 FAIL；Explorer 重启、物理多屏/负坐标、125%～200%、热插拔、休眠和时区变更 SKIP |
| `scripts/package.ps1` | PASS | 已生成便携 ZIP、Inno Setup 安装器、SHA-256 清单和 dirty/unsigned provenance |
| `scripts/smoke-release.ps1 -Version 0.22.0` | FAIL / SKIP | Authenticode 因未配置受保护证书而 SKIP；便携 `--smoke` 的非密钥资产、搜索、快捷入口、Provider、自启、帮助均 PASS，但安全存储失败并返回 2，安装、安装后启动及卸载阶段未执行 |
| signatures evidence | SKIP | 便携包内主程序与安装器均为 `NotSigned` |
| hardware evidence | SKIP | 多屏、100%～200% 全矩阵、热插拔、休眠/恢复、时区/时钟、Explorer 重启和覆盖升级需要见证实机/一次性账户环境 |
| `scripts/verify-release.ps1` | FAIL | 指定本轮证据目录后首先拒绝 dirty 工作区；即使清理工作区，UI/system/performance/package-smoke/signatures/hardware 也仍含 FAIL/SKIP |

## 3. 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.22.0-portable.zip` | 76,809,748 | `95664957721176a122b2a20ef6bd163cf0b8201d13c5e5e38d3e7bfbee1ad9ca` |
| `windows-ai-desktop-pet-v0.22.0-setup.exe` | 53,997,830 | `11f76f6fbef47517ae2c7d4952c7e5ba03dc6a2794451765470253c3f7d64a4d` |

清单为 `dist/checksums/SHA256SUMS.txt`，provenance 为 `dist/checksums/RELEASE_PROVENANCE.json`。两项哈希与本轮打包和三组资产绑定证据一致，但候选来自 dirty 工作区且未签名。工作区中用户原有的 0.19.0/0.20.0 未跟踪候选资产未被删除或改名。

## 4. 结论

UX-220-01～19 已按设计顺序实现，PRD、工程规范、技术设计、手册、测试计划和版本资料已同步，自动化回归和候选打包完成；但真实前台 UI、完整性能、安全存储、便携启动、签名和物理环境门禁存在 FAIL/SKIP，且资产不是从干净提交构建。因此本版只能保留为本地候选，**不得创建稳定 `v0.22.0` 标签或 GitHub Release**。修复环境后必须从干净提交重新打包，并完整重跑 UI、性能、系统、package-smoke、签名和硬件证据。
