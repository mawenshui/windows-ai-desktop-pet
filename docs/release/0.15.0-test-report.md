# 0.15.0 候选验证报告

日期：2026-09-08。对象：0.15.0 候选工作区。本文只记录本轮实际执行结果；0.14.0 及更早报告不作为本轮证据。

## 自动化

- 完整 AI 配置功能的定向测试：**PASS，31/31**。覆盖全字段加密往返、密文无 Key/端点、错误口令、密文篡改、跨数据目录导入后活动配置与 Key 即用、自定义 Provider 迁移，以及第二个凭据写入失败时回滚。
- `scripts/test.ps1 -CI`：**PASS**。项目结构、UTF-8、敏感文件名和发布证据反例校验通过；Release 全解决方案构建 0 警告、0 错误；xUnit **253/253**。

## 分发与环境门禁

| Gate | 当前结果 | 说明 |
| :--- | :--- | :--- |
| automated | PASS | `scripts/test.ps1 -CI` 完整通过，253/253 |
| package-smoke | PASS | 正常 Windows 登录会话的自包含便携启动、临时安装、已安装启动、卸载和自启清理通过；受限会话的凭据错误不作为通过证据 |
| system | PASS / SKIP | 真实凭据库、启动/帮助/存储/退出、8/8 几何合同、当前单屏 100% 和底部任务栏通过；需要改变设备或系统状态的项按下表 SKIP |
| ui | **FAIL** | `inline-search-scope`：当前交互桌面不能把应用置前，runner 停止且未发送输入；新口令对话框没有真实鼠标/键盘通过证据 |
| performance | **FAIL** | 20,000 条匿名索引、200 次查询 p50 3.8842 ms、p95 4.6774 ms、max 4.9363 ms；应用窗口已出现，但 UI Automation 无法定位“小方工具袋”控件 |
| signatures | SKIP | 未提供受保护代码签名证书 |
| hardware | SKIP | 多屏/缩放、休眠/时区、Explorer 重启和真实升级需物理环境 |

任何待执行、FAIL 或 SKIP 都表示完整发布门禁未通过。当前不得创建 `v0.15.0` 标签或 GitHub Release。

## EXT-11 验证边界

- 自动化使用匿名伪 Key 和内存凭据库，不读取或修改开发者真实凭据。
- Release 构建必须为 0 警告、0 错误；普通 `settings.json`、日志、诊断和维护备份仍由既有测试证明不存 Key。
- 真实 Windows 凭据管理器、文件选择器、口令输入、用户取消、跨账户迁移和导入后实际供应商请求需独立 UI/系统见证。

## 系统与物理边界

- 系统为 Windows NT 10.0.26200.0，单显示器 1920×1080，主屏 100% 缩放、底部任务栏。凭据、启动、帮助、存储和退出 smoke PASS。
- Explorer 实际重启、多显示器、负坐标、125%～200% 缩放、热插拔、休眠/唤醒、时区/时钟改变和真实跨版本升级均为 SKIP。
- 签名为 SKIP；未提供受保护代码签名证书，两个候选资产均为 unsigned。

## 候选资产

| 资产 | 字节 | SHA-256 |
| :--- | ---: | :--- |
| `windows-ai-desktop-pet-v0.15.0-portable.zip` | 76,712,961 | `3cda26d3deebbb0e31c6b435b7540c9ea74515586da6e2115caae99b97f765c1` |
| `windows-ai-desktop-pet-v0.15.0-setup.exe` | 53,925,442 | `cd8d6f6ac6b4a605c50f4a360031198777dd1e5910077a44331a22063f48b371` |

入口：[便携版](../../dist/portable/windows-ai-desktop-pet-v0.15.0-portable.zip)、[安装版](../../dist/installer/windows-ai-desktop-pet-v0.15.0-setup.exe)、[SHA256SUMS](../../dist/checksums/SHA256SUMS.txt)、[构建记录](../../dist/checksums/RELEASE_PROVENANCE.json)。便携归档 749 项，含应用、0.15.0 离线手册和 RGS manifest，无重复 `assets/pets/pets`。程序集 FileVersion 为 `0.15.0.0`。最终候选从提交 `5f9935c2e6794c4ef914f659dede07097e1b1a7c` 重建，provenance 与该提交一致；打包器在发布新资产和校验清单后采集状态，因此如实记录 `sourceTreeState: dirty`，并记录 `signed: false`。
