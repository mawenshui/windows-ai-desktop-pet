# 0.25.0 验证报告

日期：2026-09-18

版本：0.25.0

状态：功能实现、完整自动化、system 与 package-smoke 通过；UI、performance 未通过，signatures、hardware 为 SKIP，因此不创建标签或 GitHub Release。

## 范围

对应 FOCUS-01～09、QUIET-01～08、CHAR-01～06 与 AC-DATA-025。覆盖专注会话、低打扰桌宠、同屏全屏隐藏、点击穿透恢复、内置角色卡、设置 schema 6 及 FocusSessions 维护集成。

## 自动化

| 检查 | 结果 | 证据 |
| :--- | :--- | :--- |
| `scripts/validate-project.ps1 -CI` | PASS | 必需结构、0.25.0 SemVer/CHANGELOG、AI 入口、791 个 UTF-8 文本和敏感文件名检查通过 |
| 发布证据反例 | PASS | 缺失、失败、跳过、过期、不完整及版本/提交/资产不匹配均被拒绝 |
| Release build | PASS | 0 警告、0 错误 |
| xUnit | PASS | 368/368，通过 368、失败 0、跳过 0 |

## 打包与烟雾

| 检查 | 结果 | 证据 |
| :--- | :--- | :--- |
| 便携 ZIP | PASS | `dist/portable/windows-ai-desktop-pet-v0.25.0-portable.zip` 已由 `scripts/package.ps1` 生成 |
| 安装器 EXE | PASS | `dist/installer/windows-ai-desktop-pet-v0.25.0-setup.exe` 已由 Inno Setup 6.7.1 生成 |
| SHA-256 清单 | PASS | `dist/checksums/SHA256SUMS.txt` 只包含本版两个资产，实际哈希以该文件为准 |
| 便携/安装/运行/卸载 smoke | PASS | 完整用户会话中，便携启动、静默安装、安装后启动、版本读取、卸载及自启项清理通过；Windows 凭据管理器读写删除通过 |

## 真实 Windows 与发布门禁

| Gate | 结果 | 当次证据 |
| :--- | :--- | :--- |
| automated | PASS | 完整套件 368/368；构建 0 警告、0 错误 |
| ui | FAIL | Windows 前台输入保护在 `inline-search-scope` 阶段拒绝激活应用；runner 停止输入并生成脱敏报告/截图，没有把输入发给其他窗口 |
| system | PASS | `startup-help-storage-exit` 与 `multi-screen-and-scaling-geometry-contracts` 通过；额外物理项目按环境分别 PASS/SKIP |
| performance | FAIL | 当前交互桌面出现外部前台/鼠标输入竞争，200 次独立采样无法形成可信完整报告；冻结预算未被放宽，不把不完整运行写成 PASS |
| package-smoke | PASS | 证据绑定当次便携 ZIP 与安装器哈希 |
| signatures | SKIP | 安装器与便携版主程序均未签名，未配置受保护签名证书 |
| hardware | SKIP | 多显示器、100%～200% 缩放矩阵、热插拔、休眠/恢复、时区/改时、Explorer 重启及覆盖升级需要见证实机环境 |

当前机器只检测到单显示器、主屏 100% 缩放和底部任务栏；这些局部事实不替代完整 hardware gate。真实透明窗口专注交互、点击穿透恢复、同屏真全屏/最大化/副屏、200% 缩放、高对比度、休眠/改时、Explorer 重启、覆盖升级和远端资产仍不得推断为通过。

## 结论

0.25.0 的需求实现、自动化、构建、打包、安装与卸载烟雾测试已完成。由于 UI 与 performance 为 FAIL、signatures 与 hardware 为 SKIP，`verify-release.ps1` 的全部 PASS 条件不成立；本轮不创建 `v0.25.0` 标签，不上传 GitHub Release，也不创建任何预发布或预览资产。待独立交互桌面、签名证书和物理测试环境齐备后，应对同一已提交源码重新打包并重跑全部七组 gate。
