# 0.17.0 候选验证报告

日期：2026-09-11。版本：0.17.0。打包源提交：`c3366f77d4531579f392a7a1e2236b4918448165`；输入指纹：`a1a92e433e573750f4d7bbbb0d3e729ee5074531a0244fe517c0b0671bdda4d0`。环境为 Microsoft Windows NT 10.0.26200.0，证据驱动运行时 10.0.11。历史版本结果未作为本轮证据。

## 结果总表

| 门禁 | 结果 | 本轮证据 |
| :--- | :--- | :--- |
| automated | PASS | 项目结构/UTF-8/密钥文件校验、发布证据拒绝测试、Release 全解决方案构建、289/289 单元与集成测试 |
| system | PASS | 多屏/100%～200% 几何合同；应用启动、帮助、隔离存储、凭据往返、自启状态和退出 smoke |
| package-smoke | PASS | 便携解压运行、静默安装、安装后启动、静默卸载和自启项清理 |
| ui | FAIL | `inline-search-scope` 阶段无法将应用置前；runner 在发送任何鼠标/键盘输入前停止，未生成截图 |
| performance | FAIL | 当前桌面无法将应用置前，窗口输入测量未执行；匿名 20,000 项元数据搜索已完成，p50 3.6106 ms、p95 4.3721 ms、max 4.6225 ms |
| signatures | SKIP | 安装器和便携版 `WindowsAiDesktopPet.exe` 均为 `NotSigned`，未配置受保护签名证书 |
| hardware | SKIP | 多屏、100%～200% 缩放、热插拔、休眠恢复、时区/时钟、Explorer 重启和真实升级数据保留需要见证实机环境 |

完整 CI 的 Release 构建为 0 警告、0 错误。受控正文搜索的严格 UTF-8/UTF-16 BOM、非法编码、NUL、扩展名、单文件/文件数/语料上限、元数据优先排名、摘要、通配符/正则分流、范围清除、停用竞态、watcher 更新、设置持久化和 WPF 入口均有自动化覆盖。

system 首次在受限沙箱内因 Windows 凭据会话不可用而失败；随后在当前用户会话重跑，凭据 save/load/delete 往返和全部 system 必需项通过，最终绑定证据为 PASS。未执行会中断 Explorer、休眠系统或修改时区的破坏性环境操作。

## 候选资产

| 资产 | 大小 | SHA-256 | package-smoke |
| :--- | ---: | :--- | :--- |
| `windows-ai-desktop-pet-v0.17.0-portable.zip` | 76,775,799 bytes | `5a5df3fe16279fa0b9ac0d231efa2e4ce0f0474921043c34aa08ae25046ea90e` | PASS |
| `windows-ai-desktop-pet-v0.17.0-setup.exe` | 53,962,079 bytes | `a494837cd5e0cd8a2668b94cd77d44321538d6b7f4a9a6f0f08c656651b4e44c` | PASS |

`dist/checksums/SHA256SUMS.txt` 只包含上述两个当前资产；`RELEASE_PROVENANCE.json` 记录版本 0.17.0、干净源提交、win-x64 self-contained、构建时间和未签名状态。

## 发布结论

实现、自动化、system、打包和 package-smoke 已完成；独立 UI 与性能仍为 FAIL，签名与硬件仍为 SKIP。该证据集不能通过 `scripts/verify-release.ps1`，因此不得把 0.17.0 标记为稳定 GitHub Release，也不创建 `v0.17.0` 标签。候选资产可用于后续在解锁交互桌面、签名环境和见证实机上补齐同字节验证。
