# 0.18.1 候选验证报告

日期：2026-09-13。版本：0.18.1。打包源提交：`9b8b1a96e4ebac66531ebe5719b0e12568b4a5b8`；输入指纹：`272c6a8e7251aa7c9cabe318ecb78d12fceff80ec482ce51950e27874c5df24f`。环境为 Microsoft Windows NT 10.0.26200.0，证据驱动运行时 10.0.11。历史版本结果未作为本轮证据。

## 发布门禁

| 门禁 | 结果 | 证据 |
| :--- | :--- | :--- |
| automated | PASS | 项目/UTF-8 校验、发布门禁反例、Release 构建、302/302 xUnit |
| ui | FAIL | 应用无法成为前台窗口；在 `inline-search-scope` 输入前停止，未发送鼠标或键盘事件 |
| system | PASS | 多屏/缩放几何合同及启动、帮助、存储、退出 smoke 通过 |
| performance | FAIL | 应用无法成为前台窗口；真实输入前停止，未形成可接受的窗口性能报告 |
| package-smoke | PASS | 便携运行、静默安装、安装后运行、静默卸载及本安装位置自启清理通过 |
| signatures | SKIP | 安装器和便携主程序均为 `NotSigned`，当前未配置受保护签名证书 |
| hardware | SKIP | 多屏、缩放矩阵、热插拔、休眠、时区/时钟、Explorer 重启和升级保留缺少本轮见证环境 |

完整 CI 的 Release 构建为 0 警告、0 错误。302 项自动化覆盖既有主路径以及本版新增的搜索清空与快捷入口重复状态、全局快捷键/自动备份/更新设置的已保存基线、快捷入口管理器选择/边界/扫描状态、Delete 安全入口和待办空白标题保存条件。

system 和 package-smoke 首次在受限进程中因 Windows 凭据登录会话不可用而失败；在当前本机用户会话重跑后均为 PASS，最终证据文件只保留后一次结果。UI 和 performance 在本机交互权限下重跑仍无法置前，继续记录 FAIL。

## 候选资产

| 资产 | 大小 | SHA-256 | package-smoke |
| :--- | ---: | :--- | :--- |
| `windows-ai-desktop-pet-v0.18.1-portable.zip` | 76,784,507 bytes | `dbb405d7e880d9b77aa7ba224710c3a368fa4ff02baef46130c5a67233e71072` | PASS |
| `windows-ai-desktop-pet-v0.18.1-setup.exe` | 53,968,264 bytes | `1561aa4c0429b242a3739bb99ff2f21f9288313fa746f0fe1792b90fdb02d8b0` | PASS |

`dist/checksums/SHA256SUMS.txt` 只包含上述两个当前资产；`RELEASE_PROVENANCE.json` 记录版本 0.18.1、干净源提交、win-x64 self-contained、构建时间和未签名状态。package-smoke、signatures 和 hardware 证据均绑定到上述两个文件的实际哈希。

## 结论

实现、自动化、system、打包和 package-smoke 已完成；独立 UI 与性能仍为 FAIL，签名与硬件仍为 SKIP。该证据集不能通过 `scripts/verify-release.ps1`，因此不得把 0.18.1 标记为稳定 GitHub Release，也不创建 `v0.18.1` 标签。候选资产可用于后续在可置前的交互桌面、签名环境和见证实机上补齐同字节验证。
